retize_social_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'retize_user') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'retize_password') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'retize_social') }}"
      schema: staging
      threads: 4
      keepalives_idle: 0
      connect_timeout: 10
